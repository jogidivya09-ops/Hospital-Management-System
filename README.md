# Hospital-Management-System
Hospital Management System is a Java-based project developed using Java, JDBC, &amp; MySQL to manage hospital operations. It maintains patient records, doctor details, and appointment schedules. The system uses database connectivity for data storage, checks doctor availability, avoids booking conflicts, and demonstrates OOP concepts and SQL operations.
